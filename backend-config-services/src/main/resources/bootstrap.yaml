server:
  port: 8081
spring:
  application:
    name: backend-config-services
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Altobert/mingeso-microservices/tree/main/config-data          