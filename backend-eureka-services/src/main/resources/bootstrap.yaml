server:
  port: 8081
spring:
  application:
    name: backend-config-services
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/Altobert/mingeso-microservices
          search-paths: "Unidad 2/Aplicación-microservicio-avanzado-postgresql/config-data"